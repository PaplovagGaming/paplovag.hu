const STATE_PREFIX="paplovag-youtube-oauth-state:";const TOKEN_KEY="paplovag-youtube-oauth-refresh-token";const REDIRECT_URI="https://paplovag.hu/api/paplovag-youtube-oauth/callback";function storage(env){return env.MEDIA_KIT_KV||env.KV||null}function adminSecret(env){return env.PAPLOVAG_MEDIA_KIT_ADMIN_PASSWORD||env.MEDIA_KIT_ADMIN_PASSWORD||""}function base64url(bytes){let binary="";for(const byte of bytes)binary+=String.fromCharCode(byte);return btoa(binary).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"")}async function encryptionKey(secret){const material=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(`paplovag-youtube-oauth:${secret}`));return crypto.subtle.importKey("raw",material,{name:"AES-GCM"},false,["encrypt"])}async function encryptRefreshToken(token,secret){const iv=crypto.getRandomValues(new Uint8Array(12));const key=await encryptionKey(secret);const encrypted=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,new TextEncoder().encode(token));return{version:1,iv:base64url(iv),ciphertext:base64url(new Uint8Array(encrypted)),storedAt:new Date().toISOString()}}function errorPage(message,status=400){return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Paplovag YouTube OAuth</title></head><body style="font-family:system-ui;background:#061014;color:#f3fbfd;padding:40px"><h1>Paplovag YouTube OAuth</h1><p>${message}</p><p><a style="color:#5ee7f7" href="/media-kit/admin/">Vissza az adminhoz</a></p></body></html>`,{status,headers:{"Content-Type":"text/html; charset=utf-8","Cache-Control":"no-store","X-Robots-Tag":"noindex,nofollow"}})}export async function onRequestGet({request,env}){const url=new URL(request.url);if(url.searchParams.get("error"))return errorPage("A Google OAuth engedélykérés megszakadt vagy elutasításra került.");const code=url.searchParams.get("code"),state=url.searchParams.get("state");if(!code||!state)return errorPage("Hiányzó OAuth code vagy state paraméter.");const secret=adminSecret(env);if(!env.YOUTUBE_OAUTH_CLIENT_ID||!env.YOUTUBE_OAUTH_CLIENT_SECRET||!secret)return errorPage("Hiányzik egy szükséges OAuth vagy admin secret.",503);const kv=storage(env);if(!kv)return errorPage("A Media Kit KV binding nincs konfigurálva.",503);const stateKey=`${STATE_PREFIX}${state}`;const validState=await kv.get(stateKey);if(!validState)return errorPage("Az OAuth state lejárt vagy érvénytelen. Indítsd újra az összekötést.",403);await kv.delete(stateKey);const tokenResponse=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded",Accept:"application/json"},body:new URLSearchParams({code,client_id:env.YOUTUBE_OAUTH_CLIENT_ID,client_secret:env.YOUTUBE_OAUTH_CLIENT_SECRET,redirect_uri:REDIRECT_URI,grant_type:"authorization_code"})});let tokenData={};try{tokenData=await tokenResponse.json()}catch{}if(!tokenResponse.ok)return errorPage("A Google OAuth token exchange sikertelen volt. Ellenőrizd a redirect URI-t.",502);if(!tokenData.refresh_token)return errorPage("A Google nem adott refresh tokent. Indítsd újra az összekötést.",502);const encrypted=await encryptRefreshToken(tokenData.refresh_token,secret);await kv.put(TOKEN_KEY,JSON.stringify({...encrypted,scope:tokenData.scope||"",tokenType:tokenData.token_type||"Bearer"}));return Response.redirect("https://paplovag.hu/media-kit/admin/?youtube=connected",302)}