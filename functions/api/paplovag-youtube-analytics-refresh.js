const DATA_KEY="paplovag-media-kit";
const TOKEN_KEY="paplovag-youtube-oauth-refresh-token";
const REACH_HISTORY_KEY="paplovag-youtube-reach-history";
const ADMIN_COOKIE="pg_media_admin_session";
const TARGET_CHANNEL_ID="UCUEDPQyLPN5lrTH06k2oWYA";
const TIME_ZONE="Europe/Budapest";
const REACH_REPORT_TYPE_IDS=["channel_reach_basic_a1","channel_reach_combined_a1"];
const REACH_JOB_NAME="Paplovag Gaming Media Kit Reach";
const REACH_BATCH_SIZE=15;
const REACH_HISTORY_DAYS=180;
const REACH_WINDOW_DAYS=90;
const DEFAULT_DATA={version:1,updatedAt:"",stats:{subscribers:0,views28d:0,views90d:0,watchHours90d:0,impressions90d:0,ctr:0,avgViewDuration:"",subscriberGrowth90d:0},audience:{coreAgeLabel:"",coreAgeShare:0,male:0,female:0,countries:[]},contact:{email:"paplovaggaming@gmail.com",channelUrl:`https://www.youtube.com/channel/${TARGET_CHANNEL_ID}`}};
function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{"Content-Type":"application/json; charset=utf-8","Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff","X-Robots-Tag":"noindex, nofollow, noarchive","Referrer-Policy":"no-referrer"}})}
function storage(env){return env.MEDIA_KIT_KV||env.KV||null}function adminSecret(env){return env.PAPLOVAG_MEDIA_KIT_ADMIN_PASSWORD||env.MEDIA_KIT_ADMIN_PASSWORD||""}
function parseCookies(request){const result={};for(const item of(request.headers.get("Cookie")||"").split(";")){const[name,...rest]=item.trim().split("=");if(!name)continue;try{result[name]=decodeURIComponent(rest.join("="))}catch{result[name]=rest.join("=")}}return result}
function base64url(bytes){let binary="";for(const byte of bytes)binary+=String.fromCharCode(byte);return btoa(binary).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"")}function decodeBase64urlText(value){const normalized=value.replace(/-/g,"+").replace(/_/g,"/");const padded=normalized+"=".repeat((4-(normalized.length%4))%4);const binary=atob(padded);return new TextDecoder().decode(Uint8Array.from(binary,c=>c.charCodeAt(0)))}function decodeBase64urlBytes(value){const normalized=String(value||"").replace(/-/g,"+").replace(/_/g,"/");const padded=normalized+"=".repeat((4-(normalized.length%4))%4);const binary=atob(padded);return Uint8Array.from(binary,c=>c.charCodeAt(0))}
async function hmac(value,secret){const key=await crypto.subtle.importKey("raw",new TextEncoder().encode(secret),{name:"HMAC",hash:"SHA-256"},false,["sign"]);const signature=await crypto.subtle.sign("HMAC",key,new TextEncoder().encode(value));return base64url(new Uint8Array(signature))}async function verifySession(token,secret){if(!token||!secret)return false;const[encoded,signature]=String(token).split(".");if(!encoded||!signature)return false;const expected=await hmac(encoded,secret);if(signature.length!==expected.length)return false;let mismatch=0;for(let i=0;i<signature.length;i++)mismatch|=signature.charCodeAt(i)^expected.charCodeAt(i);if(mismatch!==0)return false;try{return Number(JSON.parse(decodeBase64urlText(encoded)).exp)>Math.floor(Date.now()/1000)}catch{return false}}
function secureEqual(a,b){const left=String(a||""),right=String(b||"");if(!left||!right||left.length!==right.length)return false;let mismatch=0;for(let i=0;i<left.length;i++)mismatch|=left.charCodeAt(i)^right.charCodeAt(i);return mismatch===0}
async function isAuthorized(request,env){if(await verifySession(parseCookies(request)[ADMIN_COOKIE],adminSecret(env)))return"admin";const auth=request.headers.get("Authorization")||"";const bearer=auth.startsWith("Bearer ")?auth.slice(7):"";if(env.MEDIA_KIT_CRON_SECRET&&secureEqual(bearer,env.MEDIA_KIT_CRON_SECRET))return"cron";return null}
function sameOrigin(request){const origin=request.headers.get("Origin");return !origin||origin===new URL(request.url).origin}
async function encryptionKey(secret){const material=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(`paplovag-youtube-oauth:${secret}`));return crypto.subtle.importKey("raw",material,{name:"AES-GCM"},false,["decrypt"])}async function decryptRefreshToken(record,secret){if(!record?.iv||!record?.ciphertext||!secret)throw new Error("oauth_token_unavailable");const key=await encryptionKey(secret);const decrypted=await crypto.subtle.decrypt({name:"AES-GCM",iv:decodeBase64urlBytes(record.iv)},key,decodeBase64urlBytes(record.ciphertext));return new TextDecoder().decode(decrypted)}
async function getAccessToken(env,refreshToken){const response=await fetch("https://oauth2.googleapis.com/token",{method:"POST",headers:{"Content-Type":"application/x-www-form-urlencoded",Accept:"application/json"},body:new URLSearchParams({client_id:env.YOUTUBE_OAUTH_CLIENT_ID,client_secret:env.YOUTUBE_OAUTH_CLIENT_SECRET,refresh_token:refreshToken,grant_type:"refresh_token"})});let payload={};try{payload=await response.json()}catch{}if(!response.ok||!payload.access_token)throw new Error("oauth_refresh_failed");return payload.access_token}
async function googleJson(url,accessToken,init={}){const response=await fetch(url,{...init,headers:{Authorization:`Bearer ${accessToken}`,Accept:"application/json",...(init.headers||{})}});let payload={};try{payload=await response.json()}catch{}if(!response.ok){const error=new Error(payload?.error?.message||`Google API HTTP ${response.status}`);error.status=response.status;error.reason=payload?.error?.status||payload?.error?.errors?.[0]?.reason||"google_api_error";error.payload=payload;throw error}return payload}
async function googleText(url,accessToken){const response=await fetch(url,{headers:{Authorization:`Bearer ${accessToken}`,Accept:"text/csv,text/plain,*/*"}});if(!response.ok){const text=await response.text().catch(()=>"");const error=new Error(`YouTube report download HTTP ${response.status}: ${text.slice(0,160)}`);error.status=response.status;throw error}return response.text()}
function localDateString(date=new Date()){const parts=new Intl.DateTimeFormat("en-CA",{timeZone:TIME_ZONE,year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(date);const v=Object.fromEntries(parts.map(p=>[p.type,p.value]));return`${v.year}-${v.month}-${v.day}`}function shiftDate(dateString,days){const date=new Date(`${dateString}T12:00:00Z`);date.setUTCDate(date.getUTCDate()+days);return date.toISOString().slice(0,10)}function reportObjects(report){const names=(report?.columnHeaders||[]).map(h=>h.name);return(report?.rows||[]).map(row=>Object.fromEntries(names.map((name,i)=>[name,row[i]])))}async function analyticsQuery(accessToken,{startDate,endDate,metrics,dimensions,sort,maxResults}){const params=new URLSearchParams({ids:"channel==MINE",startDate,endDate,metrics});if(dimensions)params.set("dimensions",dimensions);if(sort)params.set("sort",sort);if(maxResults)params.set("maxResults",String(maxResults));return googleJson(`https://youtubeanalytics.googleapis.com/v2/reports?${params}`,accessToken)}function secondsToDuration(seconds){const total=Math.max(0,Math.round(Number(seconds)||0));return`${Math.floor(total/60)}:${String(total%60).padStart(2,"0")}`}function round(value,digits=2){const f=10**digits;return Math.round((Number(value)||0)*f)/f}function countryName(code){try{return new Intl.DisplayNames(["en"],{type:"region"}).of(String(code||"").toUpperCase())||code}catch{return code}}async function loadStoredData(kv){try{return await kv.get(DATA_KEY,"json")||structuredClone(DEFAULT_DATA)}catch{return structuredClone(DEFAULT_DATA)}}
function parseCsv(text){const rows=[];let row=[],field="",quoted=false;const input=String(text||"").replace(/^\uFEFF/,"");for(let i=0;i<input.length;i++){const c=input[i];if(quoted){if(c==='"'){if(input[i+1]==='"'){field+='"';i++}else quoted=false}else field+=c;continue}if(c==='"')quoted=true;else if(c===","){row.push(field);field=""}else if(c==="\n"){row.push(field.replace(/\r$/,"");if(row.some(v=>v!==""))rows.push(row);row=[];field=""}else field+=c}if(field||row.length){row.push(field.replace(/\r$/,"");if(row.some(v=>v!==""))rows.push(row)}if(!rows.length)return[];const headers=rows.shift().map(v=>v.trim());return rows.map(values=>Object.fromEntries(headers.map((h,i)=>[h,values[i]??""])))}
function normalizeCtrPercent(value){const n=Number(value);if(!Number.isFinite(n)||n<0)return null;return n<=1?n*100:n}function aggregateReachCsv(text,fallbackDate){const daily={};for(const row of parseCsv(text)){if(row.channel_id&&row.channel_id!==TARGET_CHANNEL_ID)continue;const date=String(row.date||fallbackDate||"").slice(0,10);if(!/^\d{4}-\d{2}-\d{2}$/.test(date))continue;const impressions=Math.max(0,Number(row.video_thumbnail_impressions)||0);const ctr=normalizeCtrPercent(row.video_thumbnail_impressions_ctr);if(!daily[date])daily[date]={impressions:0,ctrWeighted:0};daily[date].impressions+=impressions;if(ctr!==null&&impressions>0)daily[date].ctrWeighted+=impressions*ctr}return daily}
async function listJobs(accessToken){const params=new URLSearchParams({pageSize:"100",includeSystemManaged:"true"});const payload=await googleJson(`https://youtubereporting.googleapis.com/v1/jobs?${params}`,accessToken);return payload.jobs||[]}async function listReportTypes(accessToken){const params=new URLSearchParams({pageSize:"100",includeSystemManaged:"true"});const payload=await googleJson(`https://youtubereporting.googleapis.com/v1/reportTypes?${params}`,accessToken);return payload.reportTypes||[]}
async function ensureReachJob(accessToken){const jobs=await listJobs(accessToken);let existing=jobs.find(job=>REACH_REPORT_TYPE_IDS.includes(job.reportTypeId));if(existing)return{job:existing,created:false,reportTypeId:existing.reportTypeId};const reportTypes=await listReportTypes(accessToken);const selected=REACH_REPORT_TYPE_IDS.map(id=>reportTypes.find(t=>t.id===id)).find(Boolean);if(!selected){const e=new Error("No supported reach report type is available for this channel.");e.code="reach_report_type_unavailable";throw e}if(selected.systemManaged){const e=new Error("Reach report is system-managed, but no matching job is available yet.");e.code="reach_system_job_unavailable";throw e}const job=await googleJson("https://youtubereporting.googleapis.com/v1/jobs",accessToken,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({reportTypeId:selected.id,name:REACH_JOB_NAME})});return{job,created:true,reportTypeId:selected.id}}
async function listReports(accessToken,jobId,analyticsEndDate){const payload=await googleJson(`https://youtubereporting.googleapis.com/v1/jobs/${encodeURIComponent(jobId)}/reports?pageSize=100`,accessToken);const minDate=shiftDate(analyticsEndDate,-(REACH_HISTORY_DAYS-1));const map=new Map();for(const report of payload.reports||[]){const date=String(report.startTime||"").slice(0,10);if(!date||date<minDate||date>analyticsEndDate)continue;const prev=map.get(date);if(!prev||String(report.createTime||"")>String(prev.createTime||""))map.set(date,report)}return[...map.values()].sort((a,b)=>String(b.startTime||"").localeCompare(String(a.startTime||"")))}
async function loadReachHistory(kv){try{const h=await kv.get(REACH_HISTORY_KEY,"json");if(h?.days&&typeof h.days==="object")return h}catch{}return{version:1,days:{}}}function pruneHistory(history,endDate){const min=shiftDate(endDate,-(REACH_HISTORY_DAYS-1));for(const date of Object.keys(history.days||{}))if(date<min||date>endDate)delete history.days[date]}
async function processReports(accessToken,reports,history){const pending=reports.filter(r=>{const date=String(r.startTime||"").slice(0,10);return date&&history.days?.[date]?.reportId!==r.id}).slice(0,REACH_BATCH_SIZE);let processed=0;for(const report of pending){const fallbackDate=String(report.startTime||"").slice(0,10);const daily=aggregateReachCsv(await googleText(report.downloadUrl,accessToken),fallbackDate);for(const[date,v]of Object.entries(daily))history.days[date]={impressions:Math.round(v.impressions||0),ctrWeighted:Number(v.ctrWeighted)||0,reportId:report.id||"",syncedAt:new Date().toISOString()};processed++}return processed}
function findReachWindow(history,endDate){for(let lag=0;lag<=30;lag++){const end=shiftDate(endDate,-lag),start=shiftDate(end,-(REACH_WINDOW_DAYS-1));const days=[];let complete=true;for(let i=0;i<REACH_WINDOW_DAYS;i++){const d=history.days?.[shiftDate(start,i)];if(!d){complete=false;break}days.push(d)}if(!complete)continue;const impressions=days.reduce((s,d)=>s+(Number(d.impressions)||0),0),weighted=days.reduce((s,d)=>s+(Number(d.ctrWeighted)||0),0);return{startDate:start,endDate:end,impressions:Math.round(impressions),ctr:impressions>0?round(weighted/impressions,2):0}}return null}
function classifyReachError(error){const msg=String(error?.message||"").toLowerCase();if(error?.status===429)return"rate_limited";if(msg.includes("not been used")||msg.includes("disabled")||msg.includes("service_disabled"))return"api_unavailable";if(error?.code==="reach_report_type_unavailable")return"report_type_unavailable";return"error"}
async function syncReach({accessToken,kv,analyticsEndDate,stats}){const attemptedAt=new Date().toISOString();try{const{job,created,reportTypeId}=await ensureReachJob(accessToken);if(created)return{status:"job_created",jobId:job.id,reportTypeId,lastAttemptAt:attemptedAt,daysCached:0,reportsProcessedThisRun:0};const reports=await listReports(accessToken,job.id,analyticsEndDate);const history=await loadReachHistory(kv);if(!reports.length)return{status:"waiting_for_reports",jobId:job.id,reportTypeId,lastAttemptAt:attemptedAt,daysCached:Object.keys(history.days||{}).length,reportsProcessedThisRun:0};const processed=await processReports(accessToken,reports,history);pruneHistory(history,analyticsEndDate);history.updatedAt=new Date().toISOString();await kv.put(REACH_HISTORY_KEY,JSON.stringify(history));const window=findReachWindow(history,analyticsEndDate),daysCached=Object.keys(history.days||{}).length;if(window){stats.impressions90d=window.impressions;stats.ctr=window.ctr;return{status:"ready",jobId:job.id,reportTypeId,lastAttemptAt:attemptedAt,lastSuccessAt:new Date().toISOString(),startDate:window.startDate,endDate:window.endDate,daysCached,reportsProcessedThisRun:processed}}return{status:processed>0?"backfilling":"waiting_for_reports",jobId:job.id,reportTypeId,lastAttemptAt:attemptedAt,daysCached,reportsProcessedThisRun:processed}}catch(error){return{status:classifyReachError(error),lastAttemptAt:attemptedAt,daysCached:0,reportsProcessedThisRun:0,errorStatus:Number(error.status)||null,error:String(error.message||error.reason||"reach_reporting_error").slice(0,240)}}}
async function refreshMediaKit(env){if(!env.YOUTUBE_OAUTH_CLIENT_ID||!env.YOUTUBE_OAUTH_CLIENT_SECRET||!adminSecret(env))throw new Error("oauth_secrets_missing");const kv=storage(env);if(!kv)throw new Error("storage_not_configured");const storedToken=await kv.get(TOKEN_KEY,"json");if(!storedToken)throw new Error("oauth_not_connected");const refreshToken=await decryptRefreshToken(storedToken,adminSecret(env)),accessToken=await getAccessToken(env,refreshToken),current=await loadStoredData(kv);const today=localDateString(),endDate=shiftDate(today,-1),start28=shiftDate(endDate,-27),start90=shiftDate(endDate,-89);const channelPayload=await googleJson("https://www.googleapis.com/youtube/v3/channels?part=id,statistics&mine=true&maxResults=50",accessToken);const target=(channelPayload.items||[]).find(i=>i.id===TARGET_CHANNEL_ID);if(!target)throw new Error("wrong_youtube_channel");const[report28,report90]=await Promise.all([analyticsQuery(accessToken,{startDate:start28,endDate,metrics:"views"}),analyticsQuery(accessToken,{startDate:start90,endDate,metrics:"views,estimatedMinutesWatched,averageViewDuration,subscribersGained,subscribersLost"})]);const row28=reportObjects(report28)[0],row90=reportObjects(report90)[0];if(!row28||!row90)throw new Error("analytics_core_data_unavailable");const warnings=[],next=structuredClone(current||DEFAULT_DATA);next.version=1;next.updatedAt=today;next.stats={...(current?.stats||DEFAULT_DATA.stats)};next.audience={...(current?.audience||DEFAULT_DATA.audience)};next.contact={...(current?.contact||DEFAULT_DATA.contact)};const subscriberCount=Number(target?.statistics?.subscriberCount);if(Number.isFinite(subscriberCount))next.stats.subscribers=Math.round(subscriberCount);next.stats.views28d=Math.round(Number(row28.views)||0);next.stats.views90d=Math.round(Number(row90.views)||0);next.stats.watchHours90d=Math.round((Number(row90.estimatedMinutesWatched)||0)/60);next.stats.avgViewDuration=secondsToDuration(row90.averageViewDuration);next.stats.subscriberGrowth90d=Math.round((Number(row90.subscribersGained)||0)-(Number(row90.subscribersLost)||0));try{const demographics=await analyticsQuery(accessToken,{startDate:start90,endDate,metrics:"viewerPercentage",dimensions:"ageGroup,gender"}),rows=reportObjects(demographics),total=rows.reduce((s,r)=>s+(Number(r.viewerPercentage)||0),0),core=rows.filter(r=>r.ageGroup==="age25-34"||r.ageGroup==="age35-44").reduce((s,r)=>s+(Number(r.viewerPercentage)||0),0),male=rows.filter(r=>r.gender==="male").reduce((s,r)=>s+(Number(r.viewerPercentage)||0),0),female=rows.filter(r=>r.gender==="female").reduce((s,r)=>s+(Number(r.viewerPercentage)||0),0),binary=male+female;if(total>0){next.audience.coreAgeLabel="25–44";next.audience.coreAgeShare=round(core/total*100,2)}if(binary>0){next.audience.male=round(male/binary*100,2);next.audience.female=round(female/binary*100,2)}}catch{warnings.push("demographics_preserved")}try{const geography=await analyticsQuery(accessToken,{startDate:start90,endDate,metrics:"views",dimensions:"country",sort:"-views",maxResults:6}),rows=reportObjects(geography),totalViews=Math.max(1,Number(row90.views)||0),countries=rows.filter(r=>r.country).map(r=>({name:countryName(r.country),share:round((Number(r.views)||0)/totalViews*100,2)})).filter(c=>c.share>0);if(countries.length)next.audience.countries=countries}catch{warnings.push("countries_preserved")}const reach=await syncReach({accessToken,kv,analyticsEndDate:endDate,stats:next.stats});next.youtubeReachSync=reach;if(reach.status!=="ready")warnings.push(`reach_${reach.status}`);next.youtubeSync={lastSuccessAt:new Date().toISOString(),analyticsEndDate:endDate,views28StartDate:start28,views90StartDate:start90,warnings};await kv.put(DATA_KEY,JSON.stringify(next));return{data:next,warnings}}
export async function onRequestPost({request,env}){if(!sameOrigin(request))return json({error:"invalid_origin"},403);const authorizedAs=await isAuthorized(request,env);if(!authorizedAs)return json({error:"not_authenticated"},401);try{return json({ok:true,triggeredBy:authorizedAs,...await refreshMediaKit(env)})}catch(error){console.error("Paplovag YouTube Analytics refresh failed",error.message);const code=String(error.message||"refresh_failed");const status=code==="wrong_youtube_channel"?409:code.includes("missing")||code.includes("not_configured")?503:502;return json({error:code},status)}}
