const form=document.getElementById("media-kit-login-form");const passwordInput=document.getElementById("media-kit-password");const statusEl=document.getElementById("media-kit-login-status");function setStatus(message,type=""){if(!statusEl)return;statusEl.textContent=message;statusEl.className=`pg-status ${type}`.trim()}function safeNextPath(){try{const raw=new URL(window.location.href).searchParams.get("next")||"/media-kit/";if(raw.startsWith("/media-kit")&&!raw.startsWith("/media-kit/login"))return raw}catch{}return"/media-kit/"}form?.addEventListener("submit",async event=>{event.preventDefault();setStatus("Checking password…");try{const response=await fetch("/api/paplovag-media-kit",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},credentials:"same-origin",body:JSON.stringify({action:"access_login",password:passwordInput?.value||""})});let payload={};try{payload=await response.json()}catch{}if(!response.ok){if(payload.error==="invalid_password"){setStatus("Incorrect password · Hibás jelszó","error");passwordInput?.select();return}if(payload.error==="password_not_configured"){setStatus("Media kit password protection is not configured yet.","error");return}throw new Error(payload.error||`HTTP ${response.status}`)}setStatus("Access granted · Hozzáférés engedélyezve","ok");window.location.replace(safeNextPath())}catch(error){setStatus(`Login failed: ${error.message}`,"error")}});